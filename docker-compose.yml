version: '3.11'
services:
  imagem: robcherry/docker-chromedriver:latest
  bot:
    container_name: av-container
    restart: always
    build: 
      context: .
      dockerfile: Dockerfile
    pull_policy: build